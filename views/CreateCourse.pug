doctype html
html
head 
<meta name="viewport" content="width=device-width, initial-scale=1.0">
link(rel="stylesheet", href="./stylesheets/styleCreate.css")
title Dashboard
body 
    .container
        form.small-form(action="/dashboard", method="post")
            h1 Cursalia
            div.form
                div.title Bienvenido Admin 
                .subtitle Ingrese nuevo curso
                .divf
                    input#firstname.input(type='text' name='curso' placeholder='Nombre del curso')
                    .cut.cut-short
                    label.placeholder(for='Nombre del curso') 
                .divf
                    input#psw.input(type='text'  name='Autor' placeholder='Autor del curso')
                    .cut.cut-short
                    label.placeholder(for='Autor del curso')
                .divf
                    input#firstname.input(type='text' name='Duracion' placeholder='Duracion')
                    .cut.cut-short
                    label.placeholder(for='Duracion del curso') 
                .divf
                    input#email.input(type='text' name='precio' placeholder='Precio')
                    .cut.cut-short
                    label.placeholder(for='Url curso')                 
                .divf
                    button.submit(type='text') Guardar