doctype html
html
head 
<meta name="viewport" content="width=device-width, initial-scale=1.0">
link(rel="stylesheet", href="./stylesheets/stylesingUp.css")
body 
    .container
        form.small-form(action="/auth/login", method="post")
            h1 Cursalia
            div.form
                div.title Bienvenido 
                .subtitle Registrate
                .divf
                    input#firstname.input(type='text' placeholder='Nombre')
                    .cut.cut-short
                    label.placeholder(for='firstname') 
                .divf
                    input#lastname.input(type='text' placeholder='Email')
                    .cut.cut-short
                    label.placeholder(for='Email') 
                .divf
                    input#email.input(type='password' placeholder='Contraseña')
                    .cut.cut-short
                    label.placeholder(for='psw') 
                    button.submit(type='text') Registrarme
